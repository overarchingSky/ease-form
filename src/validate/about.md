`errors.first()`: get the first error that failed to verificate.
`errors.all()`: return an error list which not be grouped.
`errors.collect()`: get the all errors grouped by field name.
`v-validate.continues`: force rules to run ,even if the field's vlaue was empty.
